/**
 * Theme module for Aura Clock Tab
 * 
 * This module handles all theme-related functionality including:
 * - Theme application and management
 * - Theme style updates
 * - Fallback theming
 */

export * from './themeManager';

// Re-export theme-related types for convenience
export type { ThemeData, ThemeName } from '../types';
